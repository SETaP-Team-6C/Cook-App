import 'package:flutter/material.dart';

// jsut a name space in rust its just {insert code}
class AppTheme { 
    static ThemeData lightTheme = ThemeData(
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.deepPurple), // builds colour pallette
        useMaterial3: true,
    );
}
